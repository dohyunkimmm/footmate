# FootMate v1.1 · Visual & UX Refinement

Date: 2026-09-17  
Branch: `feat/v1.1-visual-ux`  
Base: `main@eff58f3`

## Status

FootMate is no longer treated as frozen for current development. The 2026-09-12 close-out remains a historical QA snapshot only. v1.1 resumes iteration from the verified baseline without rewriting the approved source HTML files.

## Goal

Raise the prototype from a functionally verified portfolio demo to a more coherent product experience by improving visual hierarchy, interaction consistency, responsive behavior, and the alignment between the Live Demo and Case Study.

## Design principles

1. Preserve verified product logic and state transitions.
2. Keep `demo-source.html` and `index-source.html` as baseline source artifacts during the first refinement pass.
3. Consolidate visual tokens instead of adding screen-specific color overrides.
4. Improve the core journey first: Home → Match Detail → Payment → Game Day / Result → Profile.
5. Keep user-facing recommendation explanation separate from PM/QA evidence density.
6. Treat accessibility and reduced-motion behavior as design constraints, not post-processing.

## Implemented in the first v1.1 pass

### Demo

- Added `footmate-v1.1.css` as a dedicated top-level design layer.
- Consolidated brand, semantic color, surface, line, shadow, radius, and text tokens.
- Raised primary/secondary/social CTA height to a mobile-friendly 48px baseline.
- Unified card, profile, notification, match/status card border and shadow language.
- Refined Home filters, match-card hierarchy, Detail surface treatment, Payment rows, Profile hero/menu, runtime launchers, and Product Validation surfaces.
- Added compact viewport adaptations down to 320px without modifying product logic.

### Case Study

- Added `index-v1.1.css` for the Case Study visual layer.
- Refined navigation, cards, cover hierarchy, demo showcase device, CTA/focus behavior, and mobile layout.
- Added explicit `v1.1 · Visual & UX Refinement · iteration` status to the cover.
- Updated the decision summary to distinguish the verified functional baseline from the new visual iteration.
- Kept previous QA claims historical; v1.1 visual/mobile QA is not labeled complete until it passes.

### QA

- Expanded Playwright projects from desktop-only Chromium to:
  - Desktop Chrome
  - 390×844 Chromium viewport
  - 320×700 compact Chromium viewport
- Added `tests/e2e/v1.1-visual.spec.cjs` for:
  - design-layer loading
  - representative-screen viewport fit
  - primary touch-target height
  - Case Study v1.1 synchronization

## Technical review

The current shell/runtime architecture is intentionally preserved in this first pass to isolate visual changes from product logic. However, v1.1 confirms a structural limitation: `demo-source.html` remains a large single HTML source and runtime behavior is spread across patch/finalize/hardening layers that override globals and mutate DOM state.

A later architecture pass should consider:

- CSS token/component consolidation into fewer layers
- ES module boundaries for matching, state, persistence, UI, and validation
- reducing global function overrides such as `window.goScreen`, `window.switchDay`, and related patches
- replacing shell-time `document.write()` composition with a direct static/module entry where practical
- preserving the existing regression and Production Smoke contracts while migrating incrementally

This structural work is not required to validate the first visual pass and should not be mixed into the same change set unless a visual/UX improvement cannot be implemented safely without it.

## Validation state

Historical baseline:

- P0–P2 implementation baseline: `a6cb6fe`
- close-out `main`: `eff58f3`
- previous Regression / Chromium / axe / Production Smoke evidence remains historical baseline evidence.

v1.1:

- code and preview QA: in progress
- mobile/compact viewport QA: in progress
- Production deployment: not yet promoted
- Notion current-version documentation: to be synchronized after preview verification

## Next pass after preview QA

1. Inspect the rendered Preview for hierarchy, overflow, tap-target, and contrast regressions.
2. Fix any mobile/compact failures without altering product facts or state semantics.
3. Add or adjust tests for issues found in Preview.
4. Update Notion project status from Portfolio Freeze to active v1.1 iteration.
5. Open/complete PR only after the branch QA state is clear.
6. Promote to Production only after GitHub and Vercel checks are green.
