# FootMate 검증 가능한 완성도 · 2026-09-11

기준: GitHub `main`의 승인된 `demo.html` / `demo-source.html` 원본을 유지하고, 실제 브라우저·접근성·Production 배포를 반복 검증할 수 있는 자동 QA 레이어를 운영합니다.

## 검증 레이어

### 1. Node 회귀 테스트 · 36개

```sh
node --test tests/*.test.cjs
```

- `tests/regression.test.cjs` 20개 — Deep Link, 화면 이력, 퀴즈·타이머, 지역·결제·참가 상태, ELO, IME·키보드 등
- `tests/runtime-patch.test.cjs` 9개 — 하드 필터, 추천 정렬, Data Quality, 이벤트 순서, 상태 연결 키, 홈 필터, 크레딧 등
- `tests/quality-hardening.test.cjs` 7개 — shell/승인 원본 동기화, Production metadata, 런타임 문법, 저잔액 무부작용, ID 기반 persistence/migration

### 2. Chromium 실브라우저 E2E · 5개

`tests/e2e/footmate.spec.cjs`를 Playwright Chromium에서 실행합니다.

- Production-like `/demo` shell과 전체 39개 화면·런타임 레이어 부팅
- 실제 보이는 컨트롤을 통한 온보딩 완료
- 소개 화면 종료 후 Deep Link 복원
- `s-pay-low` 화면 이동 무부작용, 명시적 저잔액 시뮬레이션과 reload persistence
- 대표 화면 `s-splash` · `s-home` · `s-detail` · `s-pay` · `s-profile`의 axe WCAG 2 A/AA `serious`/`critical` 위반 0건 gate

브라우저 테스트는 `pageerror`, same-origin HTTP 4xx/5xx, 의미 있는 `console.error`도 실패로 처리합니다.

### 3. Production Smoke · `main` push

`Regression 36`과 `Browser E2E + axe`가 먼저 성공한 뒤, 해당 `GITHUB_SHA`의 Vercel commit status가 `success`가 될 때까지 확인합니다. 이후 실제 `https://footmate-black.vercel.app`을 대상으로 검증합니다.

- HTTP: `/`, `/demo`, `/demo-source`, `footmate-core.js`, `footmate-finalize.js`
- Production `/`의 description·canonical·Open Graph·Twitter Card marker
- Production `/demo`의 shell loader와 전체 39개 화면
- 실제 Chromium에서 Case Study와 Demo 렌더링·런타임 부팅·홈 화면 이동
- same-origin HTTP 오류·브라우저 `pageerror`·의미 있는 `console.error` 없음

Production HTTP 결과는 `test-results/production-smoke.json`으로 기록합니다.

Production 라우팅·Smoke의 첫 완전 통과 기준은 `15151a511c36e9ff21a4b499fbaa30656577a072`입니다. 이후 문서 동기화 `main` 재검증에서 발견된 홈 초록 상태 chip 대비를 보정한 최신 검증 기준은 `1a450f2f9f5b7c0a676ac26de38ada1a25510dab`이며, 이 SHA에서 `Regression 36`, `Browser E2E + axe`, `Production Smoke`가 모두 성공했습니다. `Production Smoke` 내부의 Vercel SHA 확인, 실제 Production HTTP 검사, Production Chromium 렌더 검사, 증거 Artifact 업로드도 모두 성공했습니다.

### 4. 실패 증거 Artifact

브라우저·Production QA는 성공/실패와 관계없이 GitHub Actions Artifact를 남깁니다.

- Playwright HTML report
- 실패 screenshot
- 실패 trace
- error context
- Production smoke JSON

Artifact 보존 기간은 14일입니다.

## 자동 접근성 검사에서 발견·수정한 실제 결함

axe gate를 적용하면서 테스트 자체가 아니라 UI에 존재하던 색 대비 문제를 확인했습니다. 승인 원본 HTML은 수정하지 않고 `footmate-finalize.css` 런타임 보정 레이어에서만 최소 수정했습니다.

- 네이버 로그인 버튼: 녹색 배경 위 흰색 텍스트 대비 부족 → 어두운 텍스트로 보정
- 홈 화면의 회색·파랑·초록 보조 텍스트/칩: WCAG AA 경계 미달 → 동일 색상 계열의 더 진한 runtime token으로 보정
- 팀 상세 포지션 다이어그램: 채워진/빈 포지션 텍스트 대비 부족 → field green과 filled text를 더 진한 녹색으로 보정
- 문서 동기화 후 `main` 재검증에서 홈 초록 상태 chip 2개가 4.43:1로 재현됨 → axe 기준 4.5:1을 유지한 채 `#146B4D` 런타임 색으로 추가 보정

검사 기준을 낮추거나 위반을 예외 처리하지 않고 실제 표시 색을 보정했으며, 후속 PR의 동일 axe gate에서 다시 통과했습니다.

## PR·배포 검증 이력

- PR #3 — Node 36개 회귀, Chromium E2E 5개, 대표 화면 axe gate, Production Smoke와 QA Artifact 체계를 추가. PR required checks 성공
- PR #4 — Vercel `cleanUrls`와 물리 `demo.html`의 `/demo` 충돌을 해소하고 오래된 `s-v3` smoke marker를 실제 승인 원본 marker로 교체. 승인 원본 `demo.html`·`demo-source.html`은 수정하지 않음. merge SHA `15151a5`에서 최초로 Production HTTP + Chromium Smoke까지 전체 성공
- GitHub `main` Ruleset — 사용자가 GitHub 설정에서 활성화. Pull Request를 강제하고 `Regression 36`, `Browser E2E + axe`를 required status check로 적용하며 최신 `main` 기준 재검증을 요구
- PR #5 — README를 Production QA·Ruleset 상태와 동기화. 이 docs-only merge의 `main` 재검증에서 홈 초록 상태 chip 2개의 4.43:1 대비가 axe gate에 포착됨
- PR #6 — 테스트 기준을 완화하지 않고 `footmate-finalize.css`에 홈 초록 chip 대비 보정을 추가하고 README에 후속 이력을 기록. merge SHA `1a450f2`에서 `Regression 36`, `Browser E2E + axe`, `Production Smoke` 전체 성공 및 Vercel status `success` 확인

`Production Smoke`는 PR에서 의도적으로 실행하지 않고 `main` push에서 해당 SHA의 실제 Vercel Production 배포를 대상으로 실행합니다.

## 사용자 수동 QA

사용자 확인 기준으로 다음 수동 QA에서 문제 없음을 확인한 상태를 유지합니다.

- 실제 iPhone·Android 핵심 흐름
- VoiceOver / TalkBack 실제 발화·포커스·동적 상태 알림
- 전체 39개 화면 Console Error 전수 확인

자동 axe 검사는 대표 화면의 기계 검증이며 위 수동 스크린리더 QA를 대체한다고 표현하지 않습니다.

## 현재 보호·검증 상태

- `main` Ruleset: Active
- Pull Request 필수, required approvals 0
- Required status checks: `Regression 36`, `Browser E2E + axe`
- 최신 `main` 기준 재검증 요구
- force push·branch deletion 차단, bypass 없음
- Production 검증: `main` push 후 Vercel current-SHA `success`를 확인하고 실제 HTTP + Chromium Smoke 실행

## 남은 범위

- 실제 로그인·위치·결제·DB·알림·외부 AI 모델·운영자 시스템 연동
- 실데이터 기반 정원·경기 상태 Freshness 및 서버 기준 상태 충돌 차단
- 실제 서비스 운영 시 개인정보·보안·정책·관측 지표 검증

프로토타입 범위의 검증 체계는 Node 로직 회귀 + 실제 Chromium E2E + 대표 화면 접근성 gate + 실제 Production smoke + 실패 증거 Artifact + `main` Required Checks로 구성되며, 최신 구현·자동 QA 검증 기준은 `1a450f2f9f5b7c0a676ac26de38ada1a25510dab`입니다.
