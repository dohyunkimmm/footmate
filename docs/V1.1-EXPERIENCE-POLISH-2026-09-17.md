# FootMate v1.1 · Experience Polish

Date: 2026-09-17
Base: `eff58f3`
Working branch: `feat/v1-1-experience-polish`

## Goal

기존 39개 화면과 P0–P2 기능·정책·상태 머신·이벤트/KPI 검증을 유지하면서 Visual Polish, UI/UX, Design System, responsive QA를 고도화한다.

이전 `Portfolio Freeze 2026.09.12`는 당시 검증 snapshot으로 보존하며 현재 개발 잠금 상태를 의미하지 않는다.

## v1.1 scope

### Design System
- Brand / semantic color token 정리
- text / surface / border / status color 계층 통합
- radius / shadow / touch target 규칙 통합
- Button / Card / Chip / Navigation / status UI 일관성 강화

### Core UX
- Home 경기 카드 scanability와 필터 selected state 강화
- Match Detail / Payment / Profile의 surface hierarchy 개선
- ELO / Game Day / status feedback 표현 통일
- Product Validation 패널을 앱 visual language와 정렬
- 사용자용 기능과 검증용 inspector의 시각적 역할 구분 기반 마련

### Case Study
- v1.1 version context 표시
- Experience Polish 의사결정과 범위 설명 추가
- Demo showcase presentation polish
- 2026-09-12 Portfolio Freeze를 과거 snapshot으로 명확히 분리

### QA
- 기존 Regression 36 유지
- 기존 Chromium E2E + axe gate 유지
- 320 / 375 / 390 px 대표 화면 responsive gate 추가
- 주요 CTA touch target 44px 이상 검증
- Product Validation inspector 모바일 width 검증

## Architecture policy

v1.1에서는 전면 프레임워크 전환을 하지 않는다.

기존 기능 회귀 위험을 줄이기 위해 `demo-source.html` 및 승인된 runtime 기능은 유지하고, `footmate-v1.1.css`를 독립 visual layer로 추가한다. 디자인과 UX가 안정화된 뒤 중복 CSS와 runtime override를 단계적으로 통합한다.

## Release gate

Production 승격은 아래 검증을 모두 통과한 main SHA를 기준으로 판단한다.

- Regression suite PASS
- Browser E2E PASS
- axe serious / critical 0
- 320 / 375 / 390 responsive gate PASS
- Case Study / Demo 내용 일치
- main merge 후 Production HTTP + Browser Smoke PASS

Vercel Preview는 프로젝트의 Verified Commits 정책 때문에 API/connector에서 생성한 `unverified` 커밋이 자동 취소될 수 있다. 이 경우 Preview 취소를 코드 실패로 간주하지 않고 GitHub QA와 main Production Smoke를 출고 판정 기준으로 사용한다.

## Version record

`Portfolio Freeze 2026.09.12`는 과거 검증 snapshot이다. v1.1 이후에는 현재 main SHA와 Production 검증 결과를 버전 기준으로 사용한다.
