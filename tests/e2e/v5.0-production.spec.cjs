const {test,expect}=require('@playwright/test');
test('v5.0 exact Production connected platform render',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/app',{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','5.0.0');await page.waitForFunction(()=>window.__FOOTMATE_V5__?.version==='5.0.0');await expect(page.locator('#footmate-next')).toHaveAttribute('data-connected-platform-version','5.0.0');await expect(page.locator('#footmate-next')).toHaveAttribute('data-connection-mode','mock-only');const v5=await page.evaluate(()=>({version:window.__FOOTMATE_V5__?.version,architecture:window.__FOOTMATE_V5__?.architecture,mode:window.__FOOTMATE_V5__?.connectionMode,connected:window.__FOOTMATE_V5__?.connectedProviders,external:window.__FOOTMATE_V5__?.externalProductionFeatures}));expect(v5).toEqual({version:'5.0.0',architecture:'connected-capable',mode:'mock-only',connected:[],external:false});await page.goto('/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.documentElement.dataset.footmateCaseStudyRelease==='5.0.0');await expect(page.locator('[data-v5-provider-evidence="mock-only"]')).toHaveCount(1)});
test('v5.0 exact Production aliases stay current',async({page})=>{for(const route of ['/demo','/next']){await page.goto(route,{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','5.0.0')}});
