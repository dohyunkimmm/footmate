const {test,expect}=require('@playwright/test');
const AxeBuilder=require('@axe-core/playwright').default;
async function open(page,w=390,h=844){await page.setViewportSize({width:w,height:h});await page.goto('/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.querySelectorAll('.slide').length===16&&document.documentElement.dataset.footmateCaseStudyRelease==='4.5.0')}
test('v4.5 Case Study carries Matchday Operations evidence',async({page})=>{await open(page,1440,900);await expect(page.locator('.fm-next-cover-note')).toContainText('v4.5.0');await page.evaluate(()=>window.goTo(10));await expect(page.locator('.slide.on')).toContainText('upcoming → matchday → checked-in');await page.evaluate(()=>window.goTo(11));await expect(page.locator('.slide.on')).toContainText('late → arrival recovery')});
test('v4.5 Case Study remains mobile-safe and accessible',async({page})=>{for(const w of [320,375,390,430]){await open(page,w);for(let i=0;i<16;i++){await page.evaluate(x=>window.goTo(x),i);expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1)}}const axe=await new AxeBuilder({page}).include('body').withTags(['wcag2a','wcag2aa']).analyze();expect(axe.violations.filter(v=>['serious','critical'].includes(v.impact))).toEqual([])});
