const {test,expect}=require('@playwright/test');
function capture(page){const e=[];page.on('pageerror',x=>e.push(`pageerror: ${x.message}`));page.on('console',m=>{if(m.type()==='error'&&!m.text().includes('Failed to load resource'))e.push(`console.error: ${m.text()}`)});return e}
test('v4 exact Production app and Case Study render',async({page})=>{const errors=capture(page);await page.setViewportSize({width:390,height:844});await page.goto('/app',{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','4.0.0');await expect(page.getByRole('heading',{name:/내 수준에 맞는 경기부터/})).toBeVisible();await page.getByRole('button',{name:/내 경기 찾아보기/}).click();await page.getByRole('button',{name:'다음'}).click();await page.getByRole('button',{name:'다음'}).click();await page.getByRole('button',{name:/추천 경기 보기/}).click();await expect(page.locator('[data-screen="home"]')).toBeVisible();expect(errors).toEqual([]);await page.setViewportSize({width:1440,height:900});await page.goto('/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.querySelectorAll('.slide').length===16&&document.querySelector('.fm-next-cover'));await expect(page.locator('.fm-next-cover-note')).toContainText('v4.0.0');await expect(page.locator('.fm-next-cover-frame iframe')).toHaveAttribute('src','/app?embed=1');expect(errors).toEqual([])});
test('v4 exact Production compatibility aliases no longer expose pre-v4 product',async({page})=>{for(const route of ['/demo','/next']){await page.goto(route,{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','4.0.0');await expect(page.locator('#footmate-next')).toBeVisible()}});
