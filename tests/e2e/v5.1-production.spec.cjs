const {test,expect}=require('@playwright/test');
test('v5.1.1 exact Production renders AI Match Assistant resilience boundary',async({page,request})=>{await page.setViewportSize({width:390,height:844});await page.goto('/app',{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','5.1.1');await page.waitForFunction(()=>window.__FOOTMATE_V5__?.version==='5.1.1'&&window.__FOOTMATE_AI__?.version==='5.1.1');await expect(page.locator('#footmate-next')).toHaveAttribute('data-connected-platform-version','5.1.1');await expect(page.locator('#footmate-next')).toHaveAttribute('data-ai-assistant','available');const api=await request.get('/api/ai-match-assistant');expect(api.ok()).toBe(true);const health=await api.json();expect(health.version).toBe('5.1.1');expect(health.provider).toBe('vercel-ai-gateway');expect(health.model).toBe('openai/gpt-5.4-mini');await page.goto('/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.documentElement.dataset.footmateCaseStudyRelease==='5.1.1');await expect(page.locator('[data-v5-ai-evidence="guardrailed"]')).toHaveCount(1)});
test('v5.1.1 exact Production aliases stay current',async({page})=>{for(const route of ['/demo','/next']){await page.goto(route,{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','5.1.1')}});
