const {test,expect}=require('@playwright/test');
test('v4.6 exact Production Return Loop render',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/app',{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','4.6.0');await page.evaluate(()=>{localStorage.clear();localStorage.setItem('footmate:v4:session',JSON.stringify({route:'schedule',setupComplete:true,region:'수원 · 영통',position:'MF',level:'중급',signedIn:true,joinedMatchId:'suwon-ingye-2000',selectedMatchId:'suwon-ingye-2000',matchStage:'postgame',userName:'테스터'}))});await page.reload({waitUntil:'domcontentloaded'});await expect(page.locator('[data-return-version="4.6.0"]')).toBeVisible();await page.getByRole('button',{name:'어려웠어요'}).click();await page.getByRole('button',{name:'네, 비슷한 경기'}).click();await page.getByRole('button',{name:'평가 저장'}).click();await expect(page.locator('[data-return-state="saved"]')).toBeVisible();await page.goto('/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.documentElement.dataset.footmateCaseStudyRelease==='4.6.0');await expect(page.locator('.fm-next-cover-note')).toContainText('v4.6.0')});
test('v4.6 exact Production aliases stay current',async({page})=>{for(const route of ['/demo','/next']){await page.goto(route,{waitUntil:'domcontentloaded'});await expect(page.locator('meta[name="footmate-release"]')).toHaveAttribute('content','4.6.0')}});
