const {test,expect}=require('@playwright/test');
test('v5.0 Case Study keeps 16 sections and connected platform evidence',async({page})=>{await page.goto('/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.documentElement.dataset.footmateCaseStudyRelease==='5.0.0');await expect(page.locator('.slide')).toHaveCount(16);await expect(page.locator('meta[name="footmate-case-study-release"]')).toHaveAttribute('content','5.0.0');await expect(page.locator('[data-v5-domain-evidence="separated"]')).toHaveCount(1);await expect(page.locator('[data-v5-provider-evidence="mock-only"]')).toHaveCount(1);await expect(page.locator('[data-v5-validation-evidence="acceptance"]')).toHaveCount(1);await expect(page.getByText('Connected Matchday Platform',{exact:false}).first()).toBeVisible()});
