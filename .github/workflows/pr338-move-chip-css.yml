name: PR338 Move Chip CSS
"on":
  push:
    branches:
      - fix/home-ai-compact-header-20260926
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Move chip CSS and verify targeted behavior
        run: bash .github/scripts/pr338-move-chip-css.sh
