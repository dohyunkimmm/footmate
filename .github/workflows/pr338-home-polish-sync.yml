name: PR338 Home Polish Sync
"on":
  push:
    branches:
      - fix/home-ai-compact-header-20260926
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Sync source, QA and baselines
        run: bash .github/scripts/pr338-home-polish-sync.sh
